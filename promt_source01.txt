Day 05 - prompt for idea length limitation:
Please update the 'Idea Board' logic in my UCSC Hub code to include a maximum character limit for new ideas. Specifically:
Input Constraint: Set a default limit of 200 characters for the ideaInput field using the maxlength attribute.
Visual Feedback: Add a small character counter below the input area that updates in real-time (e.g., '0 / 200') and turns red when the limit is reached.
CSS Protection: Update the .sticky-note styles to handle overflow gracefully. Ensure that if a user bypasses the limit or enters long strings, the text wraps correctly using overflow-wrap: break-word and the note stays a consistent size with overflow-y: auto.
Graceful Handling: If someone tries to add an empty idea or just spaces, show a subtle border-color change to red on the input field instead of a browser alert.
